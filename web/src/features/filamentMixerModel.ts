/**
 * Compatibility re-export for the feature preview. The generated model lives
 * in the project layer so pure Match search can use the pinned pigment model.
 */
export * from '../project/filaments/filamentMixerModel';
