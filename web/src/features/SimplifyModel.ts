export function simplifyModel(modelId: string, targetTriangleCount: number, maxErrorMm: number) {
  console.log(`Simplifying model ${modelId} to ${targetTriangleCount} triangles, max error ${maxErrorMm}mm`);
}
