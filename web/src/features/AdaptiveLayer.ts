export function computeAdaptiveLayerHeights(modelId: string, quality: number, smoothingRadius: number, keepMin: boolean) {
  console.log(`Computing adaptive layers for ${modelId}, quality ${quality}, smoothing ${smoothingRadius}, keepMin ${keepMin}`);
}
