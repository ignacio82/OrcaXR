diff --git a/CMakeLists.txt b/CMakeLists.txt
index 47ed438..5f5598d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -556,8 +556,19 @@ find_package(TBB REQUIRED)
 # The Intel TBB library will use the std::exception_ptr feature of C++11.
 # add_definitions(-DTBB_USE_CAPTURED_EXCEPTION=0)
 
-find_package(OpenSSL REQUIRED)
+# OrcaXR WASM: libslic3r's only OpenSSL use is <openssl/md5.h> (Utils.hpp,
+# Format/bbs_3mf.cpp); the WASM build injects a public-domain MD5 shim
+# header on the include path instead of cross-building all of OpenSSL.
+if (NOT EMSCRIPTEN)
+    find_package(OpenSSL REQUIRED)
+endif ()
+# OrcaXR: CURL is only consumed by the wx GUI (src/slic3r/) and the
+# profile-validator tool. libslic3r itself does not link it. Guard so
+# SLIC3R_GUI=OFF + ORCA_TOOLS=OFF builds (the WASM engine) succeed
+# without a cross-compiled libcurl.
+if (SLIC3R_GUI OR ORCA_TOOLS)
 find_package(CURL REQUIRED)
+endif ()
 if (SLIC3R_SENTRY)
     find_package(Sentry QUIET)
     if (NOT Sentry_FOUND)
@@ -569,11 +580,13 @@ endif()
 find_package(Freetype REQUIRED)
 
 
+if (SLIC3R_GUI OR ORCA_TOOLS)
 add_library(libcurl INTERFACE)
 target_link_libraries(libcurl INTERFACE CURL::libcurl)
 
 find_package(ZLIB REQUIRED)
 target_link_libraries(libcurl INTERFACE ZLIB::ZLIB)
+endif ()
 
 if (SLIC3R_SENTRY)
     add_library(sentry INTERFACE)
@@ -604,7 +617,9 @@ if (SLIC3R_SENTRY)
 endif()
 
 # Fixing curl's cmake config script bugs
-if (NOT WIN32)
+if (NOT (SLIC3R_GUI OR ORCA_TOOLS))
+    # libcurl interface target not created (WASM/headless engine build).
+elseif (NOT WIN32)
     # Required by libcurl
     #find_package(ZLIB REQUIRED)
     #target_link_libraries(libcurl INTERFACE ZLIB::ZLIB)
@@ -614,7 +629,7 @@ else()
     target_link_libraries(libcurl INTERFACE crypt32)
 endif()
 
-if (SLIC3R_STATIC AND NOT SLIC3R_STATIC_EXCLUDE_CURL)
+if (SLIC3R_STATIC AND NOT SLIC3R_STATIC_EXCLUDE_CURL AND (SLIC3R_GUI OR ORCA_TOOLS))
     if (NOT APPLE)
         # libcurl is always linked dynamically to the system libcurl on OSX.
         # On other systems, libcurl is linked statically if SLIC3R_STATIC is set.
@@ -662,6 +677,9 @@ endif ()
 find_package(PNG REQUIRED)
 
 set(OpenGL_GL_PREFERENCE "LEGACY")
+# OrcaXR: OpenGL / GLEW / glfw3 are GUI-only deps. Skip on builds
+# without the wx GUI — libslic3r itself does not link them.
+if (SLIC3R_GUI)
 find_package(OpenGL REQUIRED)
 
 set(GLEW_ROOT "${CMAKE_PREFIX_PATH}")
@@ -675,6 +693,7 @@ endif()
 find_package(GLEW REQUIRED)
 
 find_package(glfw3 REQUIRED)
+endif ()
 
 # Find the Cereal serialization library
 find_package(cereal REQUIRED)
diff --git a/deps/Boost/Boost.cmake b/deps/Boost/Boost.cmake
index d0086f2..4aef4eb 100644
--- a/deps/Boost/Boost.cmake
+++ b/deps/Boost/Boost.cmake
@@ -10,12 +10,37 @@ if (APPLE AND CMAKE_OSX_ARCHITECTURES)
     set(_context_arch_line "-DBOOST_CONTEXT_ARCHITECTURE:STRING=${CMAKE_OSX_ARCHITECTURES}")
 endif ()
 
+# OrcaXR WASM: prune Boost libs that can't build under Emscripten and that
+# libslic3r never links — context/coroutine* carry x86 asm; asio requires
+# context; process spawns children. boost_log's only asio use is its
+# non-native syslog backend, so that backend is disabled below instead.
+set(_orcaxr_boost_exclude "contract|fiber|numpy|stacktrace|wave|test")
+set(_orcaxr_boost_extra_args "")
+set(_orcaxr_boost_patch "")
+if (EMSCRIPTEN)
+    string(APPEND _orcaxr_boost_exclude
+        "|context|coroutine|coroutine2|asio|process|cobalt|graph_parallel|mpi")
+    # BOOST_LOG_NO_ASIO compiles out boost_log_setup's TCP/UDP sink-address
+    # parsing (init_from_settings.cpp), the one TU that #includes asio
+    # headers with asio excluded from the build.
+    set(_orcaxr_boost_extra_args
+        "-DBOOST_LOG_WITHOUT_SYSLOG:BOOL=ON"
+        "-DCMAKE_CXX_FLAGS:STRING=-pthread -fwasm-exceptions -DBOOST_LOG_NO_ASIO")
+    # boost_log/boost_log_setup link Boost::asio UNCONDITIONALLY even though
+    # only the (disabled) syslog backend compiles asio code — drop the
+    # dangling reference so asio/context can stay excluded.
+    set(_orcaxr_boost_patch
+        PATCH_COMMAND sed -i -e "s/Boost::asio//g" libs/log/CMakeLists.txt)
+endif ()
+
 Snapmaker_Orca_add_cmake_project(Boost
     URL "https://github.com/boostorg/boost/releases/download/boost-1.84.0/boost-1.84.0.tar.gz"
     URL_HASH SHA256=4d27e9efed0f6f152dc28db6430b9d3dfb40c0345da7342eaa5a987dde57bd95
     LIST_SEPARATOR |
+    ${_orcaxr_boost_patch}
     CMAKE_ARGS
-        -DBOOST_EXCLUDE_LIBRARIES:STRING=contract|fiber|numpy|stacktrace|wave|test
+        -DBOOST_EXCLUDE_LIBRARIES:STRING=${_orcaxr_boost_exclude}
+        ${_orcaxr_boost_extra_args}
         -DBOOST_LOCALE_ENABLE_ICU:BOOL=OFF # do not link to libicu, breaks compatibility between distros
         -DBUILD_TESTING:BOOL=OFF
         "${_context_abi_line}"
diff --git a/deps/EXPAT/EXPAT.cmake b/deps/EXPAT/EXPAT.cmake
index ee9258c..a1f42a2 100644
--- a/deps/EXPAT/EXPAT.cmake
+++ b/deps/EXPAT/EXPAT.cmake
@@ -1,7 +1,18 @@
+# OrcaXR WASM: expat's entropy-source detection finds nothing under
+# Emscripten and #errors out. Entropy only salts expat's internal hash
+# (DoS hardening for untrusted XML); we parse local project files, so the
+# fallback is acceptable. Keep -pthread (must match every other object).
+set(_orcaxr_expat_flags "")
+if (EMSCRIPTEN)
+    set(_orcaxr_expat_flags "-DCMAKE_C_FLAGS:STRING=-pthread -fwasm-exceptions -DXML_POOR_ENTROPY=1")
+endif ()
+
 Snapmaker_Orca_add_cmake_project(EXPAT
   # GIT_REPOSITORY https://github.com/nigels-com/glew.git
   # GIT_TAG 3a8eff7 # 2.1.0
   SOURCE_DIR          ${CMAKE_CURRENT_LIST_DIR}/expat
+  CMAKE_ARGS
+    ${_orcaxr_expat_flags}
 )
 
 if (MSVC)
diff --git a/deps/GMP/GMP.cmake b/deps/GMP/GMP.cmake
index 623f01a..ec6e66a 100644
--- a/deps/GMP/GMP.cmake
+++ b/deps/GMP/GMP.cmake
@@ -43,6 +43,16 @@ else ()
             set(_gmp_ccflags "${_gmp_ccflags} -mmacosx-version-min=${DEP_OSX_TARGET}")
             set(_gmp_build_tgt "--build=${_gmp_build_arch}-apple-darwin")
         endif()
+    elseif(EMSCRIPTEN)
+        # OrcaXR WASM: generic C build, no hand-tuned assembly, and a
+        # neutral host triple (autotools otherwise guesses the build
+        # machine's x86_64 and mis-probes the compiler). Also drop the
+        # C-only warning flags from CXXFLAGS -- GMP's configure runs its
+        # C++ probe with them and em++ rejects the probe.
+        # Explicit build!=host forces cross_compiling=yes so configure skips
+        # its run-the-binary probes (a WASM a.out can't exec on the host).
+        set(_gmp_build_tgt --disable-assembly --host=none --build=x86_64-pc-linux-gnu)
+        set(_gmp_ccflags "-O2 -DNDEBUG -fPIC -DPIC -pthread -fwasm-exceptions -fno-common")
     elseif(CMAKE_SYSTEM_NAME STREQUAL "Linux")
         if (${CMAKE_SYSTEM_PROCESSOR} MATCHES "arm")
             set(_gmp_ccflags "${_gmp_ccflags} -march=armv7-a") # Works on RPi-4
@@ -54,7 +64,7 @@ else ()
     endif()
 
     set(_cross_compile_arg "")
-    if (CMAKE_CROSSCOMPILING)
+    if (CMAKE_CROSSCOMPILING AND NOT EMSCRIPTEN)
         # TOOLCHAIN_PREFIX should be defined in the toolchain file
         set(_cross_compile_arg --host=${TOOLCHAIN_PREFIX})
     endif ()
@@ -65,7 +75,7 @@ else ()
         DOWNLOAD_DIR ${DEP_DOWNLOAD_DIR}/GMP
         PATCH_COMMAND git apply ${GMP_DIRECTORY_FLAG} --verbose ${CMAKE_CURRENT_LIST_DIR}/0001-GMP_GCC15.patch
         BUILD_IN_SOURCE ON
-        CONFIGURE_COMMAND  env "CFLAGS=${_gmp_ccflags}" "CXXFLAGS=${_gmp_ccflags}" ./configure ${_cross_compile_arg} --enable-shared=no --enable-cxx=yes --enable-static=yes "--prefix=${DESTDIR}" ${_gmp_build_tgt}
+        CONFIGURE_COMMAND  env "CC=${CMAKE_C_COMPILER}" "CXX=${CMAKE_CXX_COMPILER}" "CFLAGS=${_gmp_ccflags}" "CXXFLAGS=${_gmp_ccflags}" ./configure ${_cross_compile_arg} --enable-shared=no --enable-cxx=yes --enable-static=yes "--prefix=${DESTDIR}" ${_gmp_build_tgt}
         BUILD_COMMAND     make -j
         INSTALL_COMMAND   make install
     )
diff --git a/deps/JPEG/JPEG.cmake b/deps/JPEG/JPEG.cmake
index 5eca464..f37d898 100644
--- a/deps/JPEG/JPEG.cmake
+++ b/deps/JPEG/JPEG.cmake
@@ -11,6 +11,13 @@ if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
     endif ()
 endif()
 
+# OrcaXR WASM: libjpeg-turbo's SIMD paths are x86/NEON assembly — build the
+# portable C fallback under Emscripten.
+set(_orcaxr_jpeg_simd "")
+if (EMSCRIPTEN)
+    set(_orcaxr_jpeg_simd "-DWITH_SIMD:BOOL=OFF")
+endif ()
+
 Snapmaker_Orca_add_cmake_project(JPEG
     URL https://github.com/libjpeg-turbo/libjpeg-turbo/archive/refs/tags/3.0.1.zip
     URL_HASH SHA256=d6d99e693366bc03897677650e8b2dfa76b5d6c54e2c9e70c03f0af821b0a52f
@@ -18,5 +25,6 @@ Snapmaker_Orca_add_cmake_project(JPEG
     CMAKE_ARGS
         -DENABLE_SHARED=OFF
         -DENABLE_STATIC=ON
+        ${_orcaxr_jpeg_simd}
         ${jpeg_flag}
 )
diff --git a/deps/MPFR/MPFR.cmake b/deps/MPFR/MPFR.cmake
index dc45aad..ccbc1d6 100644
--- a/deps/MPFR/MPFR.cmake
+++ b/deps/MPFR/MPFR.cmake
@@ -19,8 +19,9 @@ if (MSVC)
 else ()
 
     set(_cross_compile_arg "")
-    if (CMAKE_CROSSCOMPILING)
-        # TOOLCHAIN_PREFIX should be defined in the toolchain file
+    if (CMAKE_CROSSCOMPILING AND NOT EMSCRIPTEN)
+        # TOOLCHAIN_PREFIX should be defined in the toolchain file.
+        # (EMSCRIPTEN passes --host=none via _gmp_build_tgt instead.)
         set(_cross_compile_arg --host=${TOOLCHAIN_PREFIX})
     endif ()
 
@@ -30,7 +31,7 @@ else ()
         DOWNLOAD_DIR ${DEP_DOWNLOAD_DIR}/MPFR
         BUILD_IN_SOURCE ON
         CONFIGURE_COMMAND autoreconf -f -i && 
-                          env "CFLAGS=${_gmp_ccflags}" "CXXFLAGS=${_gmp_ccflags}" ./configure ${_cross_compile_arg} --prefix=${DESTDIR} --enable-shared=no --enable-static=yes --with-gmp=${DESTDIR} ${_gmp_build_tgt}
+                          env "CC=${CMAKE_C_COMPILER}" "CXX=${CMAKE_CXX_COMPILER}" "CFLAGS=${_gmp_ccflags}" "CXXFLAGS=${_gmp_ccflags}" ./configure ${_cross_compile_arg} --prefix=${DESTDIR} --enable-shared=no --enable-static=yes --with-gmp=${DESTDIR} ${_gmp_build_tgt}
         BUILD_COMMAND make -j
         INSTALL_COMMAND make install
         DEPENDS dep_GMP
diff --git a/deps/OpenVDB/OpenVDB.cmake b/deps/OpenVDB/OpenVDB.cmake
index 911867a..2ae36a9 100644
--- a/deps/OpenVDB/OpenVDB.cmake
+++ b/deps/OpenVDB/OpenVDB.cmake
@@ -1,3 +1,11 @@
+# OrcaXR WASM: vdb_print links shmdt/SysV shared memory — not available
+# under Emscripten; the library itself builds fine.
+if (EMSCRIPTEN)
+    set(_orcaxr_vdb_print "-DOPENVDB_BUILD_VDB_PRINT=OFF")
+else ()
+    set(_orcaxr_vdb_print "-DOPENVDB_BUILD_VDB_PRINT=ON")
+endif ()
+
 if(BUILD_SHARED_LIBS)
     set(_build_shared ON)
     set(_build_static OFF)
@@ -14,6 +22,7 @@ Snapmaker_Orca_add_cmake_project(OpenVDB
     #  support vs2022, update to 8.2
     URL https://github.com/tamasmeszaros/openvdb/archive/a68fd58d0e2b85f01adeb8b13d7555183ab10aa5.zip 
     URL_HASH SHA256=f353e7b99bd0cbfc27ac9082de51acf32a8bc0b3e21ff9661ecca6f205ec1d81
+    PATCH_COMMAND git apply ${OPENVDB_DIRECTORY_FLAG} --verbose --ignore-space-change --whitespace=fix ${CMAKE_CURRENT_LIST_DIR}/0001-clang19.patch
     DEPENDS dep_TBB dep_Blosc dep_OpenEXR dep_Boost
     CMAKE_ARGS
         -DCMAKE_POSITION_INDEPENDENT_CODE=ON 
@@ -23,7 +32,16 @@ Snapmaker_Orca_add_cmake_project(OpenVDB
         -DOPENVDB_CORE_STATIC=${_build_static}
         -DOPENVDB_ENABLE_RPATH:BOOL=OFF
         -DTBB_STATIC=${_build_static}
-        -DOPENVDB_BUILD_VDB_PRINT=ON
+        ${_orcaxr_vdb_print}
+        # OrcaXR: OpenVDB 8.2's CMakeLists uses the legacy FindBoost.cmake
+        # module and searches for Boost_INCLUDE_DIR. Our Boost is 1.84
+        # installed with its modern CMake config only. Point both the
+        # legacy finder and the config-mode finder at our destdir.
+        -DBOOST_ROOT=${DESTDIR}
+        -DBoost_DIR=${DESTDIR}/lib/cmake/Boost-1.84.0
+        -DBoost_NO_SYSTEM_PATHS=ON
+        -DBoost_USE_STATIC_LIBS=ON
+        -DBoost_USE_MULTITHREADED=ON
         -DDISABLE_DEPENDENCY_VERSION_CHECKS=ON # Centos6 has old zlib
 )
 
diff --git a/deps/PNG/PNG.cmake b/deps/PNG/PNG.cmake
index 99d6e99..3385b97 100644
--- a/deps/PNG/PNG.cmake
+++ b/deps/PNG/PNG.cmake
@@ -5,6 +5,14 @@ else ()
     set(_disable_neon_extension "")
 endif ()
 
+# OrcaXR WASM: Emscripten has no separate libm (math is in libc);
+# find_library(M_LIBRARY m) comes back NOTFOUND and aborts the configure.
+# `-lm` is accepted and ignored by emcc, so just name it directly.
+set(_orcaxr_png_m "")
+if (EMSCRIPTEN)
+    set(_orcaxr_png_m "-DM_LIBRARY:STRING=m")
+endif ()
+
 if(APPLE AND IS_CROSS_COMPILE)
 # TODO: check if it doesn't create problem when compiling from arm to x86_64
     Snapmaker_Orca_add_cmake_project(PNG 
@@ -39,6 +47,7 @@ set(_patch_step "")
             -DPNG_PREFIX=prusaslicer_
             -DPNG_TESTS=OFF
             -DDISABLE_DEPENDENCY_TRACKING=OFF
+            ${_orcaxr_png_m}
             ${_disable_neon_extension}
 )
 endif()
diff --git a/deps/TBB/TBB.cmake b/deps/TBB/TBB.cmake
index 43f96b0..b738259 100644
--- a/deps/TBB/TBB.cmake
+++ b/deps/TBB/TBB.cmake
@@ -4,15 +4,30 @@ else()
     set(_patch_command "")
 endif()
 
+# OrcaXR WASM: oneTBB gained official Emscripten support in the 2021.13
+# line; 2021.5 hard-adds x86 flags (-mrtm/-mwaitpkg) because the Emscripten
+# toolchain reports CMAKE_SYSTEM_PROCESSOR=x86. Use the newer tarball for
+# WASM builds only; native builds keep the pinned 2021.5.
+if (EMSCRIPTEN)
+    set(_orcaxr_tbb_url "https://github.com/uxlfoundation/oneTBB/archive/refs/tags/v2021.13.0.zip")
+    set(_orcaxr_tbb_hash SHA256=f8dba2602f61804938d40c24d8f9b1f1cc093cd003b24901d5c3cc75f3dbb952)
+    set(_orcaxr_tbb_extra "-DTBB_STRICT:BOOL=OFF")
+else ()
+    set(_orcaxr_tbb_url "https://github.com/oneapi-src/oneTBB/archive/refs/tags/v2021.5.0.zip")
+    set(_orcaxr_tbb_hash SHA256=83ea786c964a384dd72534f9854b419716f412f9d43c0be88d41874763e7bb47)
+    set(_orcaxr_tbb_extra "")
+endif ()
+
 Snapmaker_Orca_add_cmake_project(
     TBB
-    URL "https://github.com/oneapi-src/oneTBB/archive/refs/tags/v2021.5.0.zip"
-    URL_HASH SHA256=83ea786c964a384dd72534f9854b419716f412f9d43c0be88d41874763e7bb47
+    URL "${_orcaxr_tbb_url}"
+    URL_HASH ${_orcaxr_tbb_hash}
     PATCH_COMMAND ${_patch_command}
-    CMAKE_ARGS          
+    CMAKE_ARGS
         -DTBB_BUILD_SHARED=OFF
         -DTBB_BUILD_TESTS=OFF
         -DTBB_TEST=OFF
+        ${_orcaxr_tbb_extra}
         -DCMAKE_POSITION_INDEPENDENT_CODE=ON
         -DCMAKE_DEBUG_POSTFIX=_debug
 )
diff --git a/deps/deps-linux.cmake b/deps/deps-linux.cmake
index fbe7b71..7a8809c 100644
--- a/deps/deps-linux.cmake
+++ b/deps/deps-linux.cmake
@@ -1,6 +1,27 @@
 
 set(DEP_CMAKE_OPTS "-DCMAKE_POSITION_INDEPENDENT_CODE=ON")
 
+# OrcaXR WASM: compile every dep with pthread support (Emscripten lowers
+# atomics/TLS differently per -pthread, so ALL objects in the final link
+# must agree). Threads back TBB-parallel slicing via SharedArrayBuffer;
+# the web app must be served with COOP/COEP headers.
+if (EMSCRIPTEN)
+    list(APPEND DEP_CMAKE_OPTS
+        "-DCMAKE_C_FLAGS:STRING=-pthread -fwasm-exceptions"
+        "-DCMAKE_CXX_FLAGS:STRING=-pthread -fwasm-exceptions"
+        # The Emscripten toolchain restricts find_* to its sysroot, so deps
+        # can't see the libs already installed in our DESTDIR (PNG failing
+        # to find our ZLIB, CGAL failing to find GMP, …). Same fix the
+        # Android build applies at the libslic3r stage.
+        "-DCMAKE_FIND_ROOT_PATH_MODE_PACKAGE=BOTH"
+        "-DCMAKE_FIND_ROOT_PATH_MODE_LIBRARY=BOTH"
+        "-DCMAKE_FIND_ROOT_PATH_MODE_INCLUDE=BOTH"
+        # Host CMake 4.x refuses sub-projects whose cmake_minimum_required
+        # is < 3.5 (vendored expat & co) without this escape hatch.
+        "-DCMAKE_POLICY_VERSION_MINIMUM=3.5"
+    )
+endif ()
+
 include("deps-unix-common.cmake")
 
 # Some Linuxes may have very old libpng, so it's best to bundle it instead of relying on the system version.
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 257b15e..ba3c7cc 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -10,11 +10,15 @@ add_subdirectory(dev-utils)
 # Note: semver and hints are now included from deps_src/CMakeLists.txt
 
 # Add MQTT library
+# OrcaXR: MQTT (Snapmaker cloud transport) is consumed by the wx GUI only;
+# it drags in OpenSSL, which the WASM/headless engine build doesn't have.
+if (SLIC3R_GUI)
 add_subdirectory(mqtt)
 
 # Set MQTT paths for use in slic3r GUI
 set(PAHO_MQTT_CPP_PATH "${CMAKE_CURRENT_SOURCE_DIR}/mqtt" CACHE PATH "Paho MQTT C++ path")
 set(PAHO_MQTT_C_PATH "${CMAKE_CURRENT_SOURCE_DIR}/mqtt/externals/paho-mqtt-c" CACHE PATH "Paho MQTT C path")
+endif ()
 
 add_subdirectory(libslic3r)
 
diff --git a/src/libslic3r/AABBTreeLines.hpp b/src/libslic3r/AABBTreeLines.hpp
index 55cf328..a01f1a6 100644
--- a/src/libslic3r/AABBTreeLines.hpp
+++ b/src/libslic3r/AABBTreeLines.hpp
@@ -32,7 +32,13 @@ namespace AABBTreeLines {
             {
                 Vec<LineType::Dim, typename LineType::Scalar> nearest_point;
                 const LineType& line = lines[primitive_index];
-                squared_distance = line_alg::distance_to_squared(line, origin.template cast<typename LineType::Scalar>(), &nearest_point);
+                // OrcaXR: materialize the cast expression into the exact
+                // Vec<Dim,Scalar> type (Slic3r's Vec = Matrix with DontAlign
+                // options) — clang 21 (Emscripten) doesn't deduce the dependent
+                // parameter from a CwiseUnaryOp/plain-eval Matrix.
+                const Vec<LineType::Dim, typename LineType::Scalar> origin_cast =
+                    origin.template cast<typename LineType::Scalar>();
+                squared_distance = line_alg::distance_to_squared(line, origin_cast, &nearest_point);
                 return nearest_point.template cast<ScalarType>();
             }
         };
diff --git a/src/libslic3r/Arachne/SkeletalTrapezoidation.cpp b/src/libslic3r/Arachne/SkeletalTrapezoidation.cpp
index 2d2cf98..ac29e83 100644
--- a/src/libslic3r/Arachne/SkeletalTrapezoidation.cpp
+++ b/src/libslic3r/Arachne/SkeletalTrapezoidation.cpp
@@ -1668,7 +1668,12 @@ SkeletalTrapezoidation::Beading SkeletalTrapezoidation::interpolate(const Beadin
     // TODO: don't use toolpath locations past the middle!
     // TODO: stretch bead widths and locations of the higher bead count beading to fit in the left over space
     coord_t next_inset_idx;
-    for (next_inset_idx = left.toolpath_locations.size() - 1; next_inset_idx >= 0; next_inset_idx--)
+    // NOTE: cast size() to signed coord_t *before* subtracting. On 32-bit targets
+    // (wasm32) size_t is 32-bit, so an empty vector's `size() - 1` underflows to
+    // 0xFFFFFFFF and zero-extends into the 64-bit signed coord_t as +4294967295,
+    // making this loop index far out of bounds. Signed subtraction yields -1 and
+    // the loop is correctly skipped on every target. (No effect on 64-bit builds.)
+    for (next_inset_idx = coord_t(left.toolpath_locations.size()) - 1; next_inset_idx >= 0; next_inset_idx--)
     {
         if (switching_radius > left.toolpath_locations[next_inset_idx])
         {
diff --git a/src/libslic3r/CMakeLists.txt b/src/libslic3r/CMakeLists.txt
index d830c5e..e8613c9 100644
--- a/src/libslic3r/CMakeLists.txt
+++ b/src/libslic3r/CMakeLists.txt
@@ -466,6 +466,18 @@ if (APPLE)
         )
 endif ()
 
+if (EMSCRIPTEN)
+    list(REMOVE_ITEM lisbslic3r_sources
+        GCode/PostProcessor.cpp
+        Format/STEP.cpp
+        Format/DRC.cpp
+        Format/svg.cpp
+        Shape/TextShape.cpp
+        ObjColorUtils.cpp
+    )
+    list(APPEND lisbslic3r_sources orcaxr_wasm_stubs.cpp)
+endif ()
+
 add_library(libslic3r STATIC ${lisbslic3r_sources} 
     ${OpenVDBUtils_SOURCES})
 source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${lisbslic3r_sources})
@@ -478,7 +490,11 @@ set(CGAL_DO_NOT_WARN_ABOUT_CMAKE_BUILD_TYPE ON CACHE BOOL "" FORCE)
 cmake_policy(PUSH)
 cmake_policy(SET CMP0011 NEW)
 find_package(CGAL REQUIRED)
-find_package(OpenCV REQUIRED core)
+# OrcaXR WASM: OpenCV backs only ObjColorUtils (obj-color quantization),
+# excluded from the web build.
+if (NOT EMSCRIPTEN)
+    find_package(OpenCV REQUIRED core)
+endif ()
 cmake_policy(POP)
 
 add_library(libslic3r_cgal STATIC 
@@ -519,10 +535,14 @@ target_compile_definitions(libslic3r PUBLIC -DUSE_TBB -DTBB_USE_CAPTURED_EXCEPTI
 target_include_directories(libslic3r PUBLIC ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_BINARY_DIR})
 target_include_directories(libslic3r SYSTEM PUBLIC ${EXPAT_INCLUDE_DIRS})
 
-# Find the OCCT and related libraries
-set(OpenCASCADE_DIR "${CMAKE_PREFIX_PATH}/lib/cmake/occt")
-find_package(OpenCASCADE REQUIRED)
-target_include_directories(libslic3r SYSTEM PUBLIC ${OpenCASCADE_INCLUDE_DIR})
+# Find the OCCT and related libraries.
+# OrcaXR WASM: OCCT (STEP import, TextShape) is not cross-compiled; its
+# entry points are stubbed in orcaxr_wasm_stubs.cpp.
+if (NOT EMSCRIPTEN)
+    set(OpenCASCADE_DIR "${CMAKE_PREFIX_PATH}/lib/cmake/occt")
+    find_package(OpenCASCADE REQUIRED)
+    target_include_directories(libslic3r SYSTEM PUBLIC ${OpenCASCADE_INCLUDE_DIR})
+endif ()
 
 find_package(JPEG REQUIRED)
 
@@ -556,13 +576,19 @@ set(OCCT_LIBS
 )
 
 find_package(libnoise REQUIRED)
+if (EMSCRIPTEN)
+    set(OCCT_LIBS "")
+    set(ORCAXR_OPENCV_LIBS "")
+else ()
+    set(ORCAXR_OPENCV_LIBS opencv_world)
+endif ()
 target_link_libraries(libslic3r
     PUBLIC
         admesh
         libigl
         libnest2d
         miniz
-        opencv_world
+        ${ORCAXR_OPENCV_LIBS}
     PRIVATE
         ${CMAKE_DL_LIBS}
         ${EXPAT_LIBRARIES}
@@ -588,7 +614,9 @@ target_link_libraries(libslic3r
 if(NOT WIN32)
     # Link freetype for OCCT dependency (CAD operations need font rendering)
     target_link_libraries(libslic3r PRIVATE ${FREETYPE_LIBRARIES})
-    target_link_libraries(libslic3r PRIVATE OpenSSL::Crypto)
+    if (NOT EMSCRIPTEN)
+        target_link_libraries(libslic3r PRIVATE OpenSSL::Crypto)
+    endif ()
     if (NOT APPLE)
         target_link_libraries(libslic3r PRIVATE fontconfig)
     endif()
diff --git a/src/libslic3r/Format/STEP.hpp b/src/libslic3r/Format/STEP.hpp
index 85c8f65..78109df 100644
--- a/src/libslic3r/Format/STEP.hpp
+++ b/src/libslic3r/Format/STEP.hpp
@@ -1,11 +1,17 @@
 #ifndef slic3r_Format_STEP_hpp_
 #define slic3r_Format_STEP_hpp_
+// OrcaXR WASM: OCCT is not cross-compiled for the web build. This header
+// leaks into most of libslic3r via Model.hpp, so the OCCT includes and the
+// one OCCT-derived class are compiled out; STEP.cpp (the only real user)
+// is excluded from the WASM source list.
+#ifndef __EMSCRIPTEN__
 #include "XCAFDoc_DocumentTool.hxx"
 #include "XCAFApp_Application.hxx"
 #include "XCAFDoc_ShapeTool.hxx"
+#include <Message_ProgressIndicator.hxx>
+#endif
 #include <boost/filesystem/path.hpp>
 #include <boost/filesystem.hpp>
-#include <Message_ProgressIndicator.hxx>
 #include <atomic>
 
 namespace fs = boost::filesystem;
@@ -25,6 +31,7 @@ const int LOAD_STEP_STAGE_UNIT_NUM           = 5;
 typedef std::function<void(int load_stage, int current, int total, bool& cancel)> ImportStepProgressFn;
 typedef std::function<void(bool isUtf8)> StepIsUtf8Fn;
 
+#ifndef __EMSCRIPTEN__
 struct NamedSolid
 {
     NamedSolid(const TopoDS_Shape& s,
@@ -35,6 +42,12 @@ struct NamedSolid
     int tri_face_cout = 0;
 };
 
+#else
+// Forward declaration only: WASM keeps signatures (Model.hpp references
+// Slic3r::Step in a std::function parameter) without OCCT types.
+class Step;
+#endif // !__EMSCRIPTEN__
+
 //BBS: Load an step file into a provided model.
 extern bool load_step(const char *path, Model *model,
                       bool& is_cancel,
@@ -70,6 +83,7 @@ private:
     EncodedType m_encode_type = EncodedType::UTF8;
 };
 
+#ifndef __EMSCRIPTEN__
 class StepProgressIncdicator : public Message_ProgressIndicator
 {
 public:
@@ -83,7 +97,9 @@ public:
 private:
     std::atomic<bool>& should_stop;
 };
+#endif // !__EMSCRIPTEN__
 
+#ifndef __EMSCRIPTEN__
 class Step
 {
 public:
@@ -104,6 +120,7 @@ private:
     Handle(XCAFDoc_ShapeTool) m_shape_tool;
     std::vector<NamedSolid> m_name_solids;
 };
+#endif // !__EMSCRIPTEN__
 
 }; // namespace Slic3r
 
diff --git a/src/libslic3r/GCode.cpp b/src/libslic3r/GCode.cpp
index 946a89e..4077b57 100644
--- a/src/libslic3r/GCode.cpp
+++ b/src/libslic3r/GCode.cpp
@@ -3055,6 +3055,14 @@ void GCode::_do_export(Print& print, GCodeOutputStream& file, ThumbnailsGenerato
         file.write("\n");
         file.write("; CONFIG_BLOCK_START\n");
         std::string full_config;
+        // OrcaXR WASM: this used to be skipped under Emscripten because
+        // serializing an enum option whose name table was never populated
+        // dereferenced a null map. fixup_enum_keys_map() now runs on the slice
+        // config at both wasm entry points, so the dump is safe — and
+        // load-bearing: without it the CONFIG_BLOCK carries only the
+        // first_layer_* scalars, and nothing downstream (our own G-code
+        // re-import, the send-time filament mapping, desktop diffing) can
+        // recover filament_colour / filament_type / layer_height.
         append_full_config(print, full_config);
         if (!full_config.empty())
             file.write(full_config);
diff --git a/src/libslic3r/GCode.hpp b/src/libslic3r/GCode.hpp
index 940bebf..f6bcf5d 100644
--- a/src/libslic3r/GCode.hpp
+++ b/src/libslic3r/GCode.hpp
@@ -179,7 +179,7 @@ struct LayerResult {
     // It is used for the pressure equalizer because it needs to buffer one layer back.
     bool        nop_layer_result { false };
 
-    static LayerResult make_nop_layer_result() { return {"", std::numeric_limits<coord_t>::max(), false, false, true}; }
+    static LayerResult make_nop_layer_result() { return {"", std::numeric_limits<size_t>::max(), false, false, true}; }
 };
 
 class GCode {
diff --git a/src/libslic3r/Model.cpp b/src/libslic3r/Model.cpp
index fbbb673..77c94d2 100644
--- a/src/libslic3r/Model.cpp
+++ b/src/libslic3r/Model.cpp
@@ -186,6 +186,13 @@ Model Model::read_from_step(const std::string&
                             double                                                  angle_defletion,
                             bool                                                   is_split_compound)
 {
+#ifdef __EMSCRIPTEN__
+    // OrcaXR WASM: OCCT/STEP is not part of the web build (v1 scope:
+    // STL/3MF/OBJ). Matches the LOAD_ERROR path of the native build.
+    (void)options; (void)stepFn; (void)stepIsUtf8Fn; (void)step_mesh_fn;
+    (void)linear_defletion; (void)angle_defletion; (void)is_split_compound;
+    throw Slic3r::RuntimeError(_L("STEP import is not available in the OrcaXR web build."));
+#else
     Model model;
     bool result = false;
     bool is_cb_cancel = false;
@@ -221,6 +228,7 @@ Model Model::read_from_step(const std::string&
         model.add_default_instances();
 
     return model;
+#endif // !__EMSCRIPTEN__
 }
 
 // BBS: add part plate related logic
diff --git a/src/libslic3r/Platform.cpp b/src/libslic3r/Platform.cpp
index 3387521..2d242e9 100644
--- a/src/libslic3r/Platform.cpp
+++ b/src/libslic3r/Platform.cpp
@@ -86,6 +86,13 @@ void detect_platform()
     BOOST_LOG_TRIVIAL(info) << "Platform: OpenBSD";
 	s_platform 		  = Platform::BSDUnix;
 	s_platform_flavor = PlatformFlavor::OpenBSD;
+#elif defined(__EMSCRIPTEN__)
+    // OrcaXR WASM: report as generic Linux — Emscripten's libc emulates a
+    // POSIX/Linux-ish environment (MEMFS /proc doesn't exist, so the
+    // Chromium/WSL flavor probes above would be meaningless anyway).
+    BOOST_LOG_TRIVIAL(info) << "Platform: WebAssembly (Emscripten)";
+	s_platform 		  = Platform::Linux;
+	s_platform_flavor = PlatformFlavor::GenericLinux;
 #else
 	// This should not happen.
     BOOST_LOG_TRIVIAL(info) << "Platform: Unknown";
diff --git a/src/libslic3r/PrintConfig.cpp b/src/libslic3r/PrintConfig.cpp
index c0a2676..b334e66 100644
--- a/src/libslic3r/PrintConfig.cpp
+++ b/src/libslic3r/PrintConfig.cpp
@@ -493,6 +493,10 @@ PrintConfigDef::PrintConfigDef()
     assign_printer_technology_to_unknown(this->options, ptAny);
     this->init_fff_params();
     this->init_extruder_option_keys();
+    // OrcaXR: upstream defines init_filament_option_keys() but never calls
+    // it, leaving m_filament_option_keys empty — which turns
+    // DynamicPrintConfig::set_num_filaments() into a silent no-op.
+    this->init_filament_option_keys();
     assign_printer_technology_to_unknown(this->options, ptFFF);
     this->init_sla_params();
     assign_printer_technology_to_unknown(this->options, ptSLA);
@@ -7379,8 +7383,15 @@ void DynamicPrintConfig::normalize_fdm(int used_filaments)
         }
     }
 
-    if (this->has("wipe_tower_filament")) {
+    if (this->has("wipe_tower_filament") && this->has("nozzle_diameter")) {
         // If invalid, replace with 0.
+        // OrcaXR: guard on nozzle_diameter being present — normalize_fdm
+        // also runs on PARTIAL configs (a process preset loaded on its own
+        // by the CLI, a per-object config), where the unguarded
+        // opt("nozzle_diameter")->size() dereferences a null option and
+        // segfaults the config loader. Without the machine's nozzle count
+        // the clamp cannot be evaluated; leave the value for the full-config
+        // normalization to validate.
         int extruder      = this->opt<ConfigOptionInt>("wipe_tower_filament")->value;
         int num_extruders = this->opt<ConfigOptionFloats>("nozzle_diameter")->size();
         if (extruder < 0 || extruder > num_extruders)
diff --git a/src/libslic3r/Thread.cpp b/src/libslic3r/Thread.cpp
index 3030b6d..4c383ec 100644
--- a/src/libslic3r/Thread.cpp
+++ b/src/libslic3r/Thread.cpp
@@ -216,6 +216,14 @@ void name_tbb_thread_pool_threads_set_locale()
 		return;
 	initialized = true;
 
+#ifdef __EMSCRIPTEN__
+	// OrcaXR WASM: this function parks one iteration on EVERY TBB pool
+	// thread via a condition-variable barrier — a guaranteed deadlock
+	// under Emscripten (and pointless: thread names are cosmetic and the
+	// locale is always "C" in WASM). First call in Print::process().
+	return;
+#endif
+
 	// see GH issue #5661 PrusaSlicer hangs on Linux when run with non standard task affinity
 	// TBB will respect the task affinity mask on Linux and spawn less threads than std::thread::hardware_concurrency().
 //	const size_t nthreads_hw = std::thread::hardware_concurrency();
